# Audit finding L11: loose version ranges with no lockfile — a fresh install can
# resolve different exact versions than what's deployed and tested, elsewhere. A
# proper lockfile needs generating from a clean, isolated venv (this repo doesn't
# ship one; the last one used here had drifted from runtime.txt's pin — see the
# manage.py check added for M24) — a `pip freeze` from a shared/system Python
# environment would capture unrelated OS packages alongside the real
# dependencies, which is worse than no lockfile at all. To generate one properly:
#   python3.13 -m venv /tmp/lockenv && /tmp/lockenv/bin/pip install pip-tools
#   /tmp/lockenv/bin/pip-compile requirements.txt --output-file=requirements-lock.txt
# Commit the result and add `pip install -r requirements-lock.txt` to setup.sh /
# the Render build command in place of requirements.txt.
Django>=4.2,<5.0
djangorestframework>=3.14,<4.0
psycopg2-binary>=2.9,<3.0
django-cors-headers>=4.3,<5.0
djangorestframework-simplejwt>=5.3,<6.0
pandas>=2.1,<3.0
requests>=2.31,<3.0
gunicorn>=21.2,<23.0
whitenoise>=6.6.0,<7.0.0
dj-database-url>=2.1.0,<3.0.0
python-dotenv>=1.0,<2.0
apscheduler>=3.10,<4.0
websocket-client>=1.6.0
# Audit fix M22: optional error tracking — no-op unless SENTRY_DSN is set in the
# deployment env, see config/settings.py. Installed unconditionally so the account
# owner can activate it with just an env var, no redeploy-time dependency change.
sentry-sdk>=2.0,<3.0
